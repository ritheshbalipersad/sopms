@model SOPMSApp.Models.Area
@{
    ViewData["Title"] = "Add Area";
}

<div class="container-fluid py-4">
    <h2 class="mb-4"><i class="fas fa-map-marked-alt me-2"></i>Add Area</h2>

    <div class="card shadow-sm" style="max-width: 500px;">
        <div class="card-body">
            <form asp-area="Admin" asp-controller="AreasAdmin" asp-action="Create" method="post">
                <div class="mb-3">
                    <label asp-for="AreaName" class="form-label">Area Name</label>
                    <input asp-for="AreaName" class="form-control" placeholder="e.g. Production" />
                    <span asp-validation-for="AreaName" class="text-danger small"></span>
                </div>
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Create</button>
                    <a asp-area="Admin" asp-controller="AreasAdmin" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
